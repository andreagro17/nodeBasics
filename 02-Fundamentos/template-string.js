const nombre = 'Deadpool'
const real = 'Wade Winston'

const normal = nombre + ' ' + real

console.log(normal)

// template strings
console.log(`${real} ${nombre}`)

const html = `
<h1> HOLA </h1>
<p> mundo </p>
`

console.log(html)